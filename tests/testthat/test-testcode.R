# practing test-that

# setting up: run this in a new package (only need to run once)
usethis::use_testthat(3)

# for each function defined in /R, have a corresponding test
# saved in the testthat/ directory
# these file names must begin with "test"

# can use these commands to create the R and test files:
# use_r("function.R")
# use_test("test-function.R")

